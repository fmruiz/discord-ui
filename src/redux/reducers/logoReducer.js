const initialState = {
  logo: [],
  loading: false,
  error: false,
};

export default function logoReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
