const initialState = {
  adminInfo: [],
  loading: false,
  error: false,
};

export default function adminInfoReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
