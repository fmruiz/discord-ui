const initialState = {
  channelImages: [],
  loading: false,
  error: false,
};

export default function channelImagesReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
