const initialState = {
  adminLogo: [],
  loading: false,
  error: false,
};

export default function adminLogoReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
