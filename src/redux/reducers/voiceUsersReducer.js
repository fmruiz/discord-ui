const initialState = {
  voiceUsers: [],
  loading: false,
  error: false,
};

export default function voiceUsersReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
