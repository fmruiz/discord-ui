const initialState = {
  musicPublications: [],
  loading: false,
  error: false,
};

export default function musicPublicationsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
