import React from "react";
import { Container, Input } from "./styles";

const InputChannels = (props) => {
  return (
    <Container>
      <Input placeholder="Message"/>
    </Container>
  );
};

export default InputChannels;
