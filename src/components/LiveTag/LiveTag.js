import React from 'react'
import { LiveTagContainer, LiveTagText } from './styles'

export const LiveTag = () => {
    return (
        <LiveTagContainer>
            <LiveTagText>LIVE</LiveTagText>
        </LiveTagContainer>
    )
}
